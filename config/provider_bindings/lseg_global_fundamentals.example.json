{
  "allow_insecure_local": false,
  "base_url": "https://replace-with-reviewed-normalization-gateway.example.com/lseg-global-fundamentals/",
  "bindings": [
    {
      "availability_basis": "provider_timestamp",
      "available_at_path": "meta.available_at",
      "dataset_type": "fundamentals",
      "limitations": [
        "Deployment template only. Replace the gateway URL and response paths with reviewed, licensed provider semantics before activation."
      ],
      "method": "GET",
      "observed_at_path": "meta.observed_at",
      "path": "v1/fundamentals/{symbol}",
      "payload_path": "data",
      "provider_record_id_path": "meta.request_id",
      "quality_state": "live",
      "query_parameters": {
        "as_of": "{as_of}",
        "end_at": "{end_at}",
        "limit": "{limit}",
        "start_at": "{start_at}"
      },
      "timeout_seconds": 30
    },
    {
      "availability_basis": "provider_timestamp",
      "available_at_path": "meta.available_at",
      "dataset_type": "filings",
      "limitations": [
        "Deployment template only. Replace the gateway URL and response paths with reviewed, licensed provider semantics before activation."
      ],
      "method": "GET",
      "observed_at_path": "meta.observed_at",
      "path": "v1/filings/{symbol}",
      "payload_path": "data",
      "provider_record_id_path": "meta.request_id",
      "quality_state": "live",
      "query_parameters": {
        "as_of": "{as_of}",
        "end_at": "{end_at}",
        "limit": "{limit}",
        "start_at": "{start_at}"
      },
      "timeout_seconds": 30
    }
  ],
  "credential_environment_variables": [
    "CAPITAL_INTELLIGENCE_LSEG_FUNDAMENTALS_API_KEY"
  ],
  "default_headers": {
    "Accept": "application/json",
    "Authorization": "Bearer ${CAPITAL_INTELLIGENCE_LSEG_FUNDAMENTALS_API_KEY}"
  },
  "provider_identifier": "lseg-global-fundamentals",
  "schema_version": "configured-dataset-provider.v1",
  "source_version": "replace-with-certified-provider-version"
}
