name: Export paper-readiness v2 source

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: read

jobs:
  export:
    if: github.head_ref == 'agent/all-markets-paper-ready-v2'
    runs-on: ubuntu-latest
    steps:
      - name: Check out current main
        uses: actions/checkout@v4
        with:
          ref: main
      - name: Package source
        run: |
          zip -qr /tmp/capital-intelligence-platform-current-main.zip . -x '.git/*'
      - name: Upload source archive
        uses: actions/upload-artifact@v4
        with:
          name: paper-readiness-v2-current-main
          path: /tmp/capital-intelligence-platform-current-main.zip
          retention-days: 1
