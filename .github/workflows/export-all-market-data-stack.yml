name: Export all-market data stack source

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
      - reopened

permissions:
  contents: read

jobs:
  export:
    if: github.head_ref == 'agent/export-all-market-data-stack'
    runs-on: ubuntu-latest
    steps:
      - name: Check out current main
        uses: actions/checkout@v4
        with:
          ref: main
      - name: Package source
        run: |
          git rev-parse HEAD > /tmp/current-main-sha.txt
          zip -qr /tmp/all-market-data-stack-current-main.zip . -x '.git/*'
      - name: Upload source archive
        uses: actions/upload-artifact@v4
        with:
          name: all-market-data-stack-current-main
          path: |
            /tmp/all-market-data-stack-current-main.zip
            /tmp/current-main-sha.txt
          retention-days: 1
