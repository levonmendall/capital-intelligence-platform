diff --git a/UI_REDESIGN_IMPLEMENTATION_SUMMARY.md b/UI_REDESIGN_IMPLEMENTATION_SUMMARY.md
new file mode 100644
index 0000000000000000000000000000000000000000..a9f295b0637a38ae9cd02799e0c95285a20a7372
--- /dev/null
+++ b/UI_REDESIGN_IMPLEMENTATION_SUMMARY.md
@@ -0,0 +1,47 @@
+# Portfolio-First Interface Redesign
+
+## Objective
+
+The interface now explains markets, economic conditions, and daily events through one consistent question: **what does this mean for the governed portfolio?** The presentation remains educational and plain-language while preserving the CIO process as the only authority for portfolio conclusions and paper implementation.
+
+## Today surface
+
+The daily information block is now organized as an investment-learning chain:
+
+1. **What changed** — the few material daily developments.
+2. **Why investors care** — the return, risk, rate, liquidity, or earnings channel involved.
+3. **Portfolio effect** — the current implication for the governed portfolio.
+4. **CIO response** — the portfolio action or deliberate inaction.
+5. **What to watch next** — the evidence that could confirm or weaken the interpretation.
+
+Full developments, source attribution, timestamps, and audit references remain available in collapsed detail.
+
+## Environment surface
+
+The economic briefing now presents the core readings most relevant to investing:
+
+- Inflation
+- Unemployment
+- Federal funds rate
+- 10-year minus 2-year Treasury spread
+
+The synopsis explains how growth reaches earnings, inflation reaches purchasing power and margins, and policy rates reach financing costs and discount rates. Economic evidence remains an input to governed comparison rather than a standalone trade signal.
+
+## History surface
+
+Operating reports, historical learning, and report archives now use the same compact card language as the primary surfaces. Long replay tables and execution-attempt records are collapsed by default. Historical evidence is explicitly framed as research and governance input that may inform review or reduce confidence and size, but cannot authorize execution or override the live CIO process.
+
+## Visual system
+
+- Permanent dark appearance
+- Cleaner segmented navigation
+- Tighter mobile spacing and card geometry
+- More restrained gradients, grid texture, and glow
+- Improved muted-text contrast
+- Consistent portfolio-lens panels across daily and economic content
+- Compact two-column metric layouts on mobile where appropriate
+- Technical detail remains accessible without dominating the default experience
+
+## Authority boundary
+
+These changes affect presentation and educational interpretation only. They do not alter candidate qualification, CIO authority, construction, execution controls, governance, or real-money permissions.
diff --git a/app.py b/app.py
index 9c8a3de3338a0a4700a3cfba9f652540d52c9a14..7936fe7efedc9cde6e8b0d08fda85c4d69212b32 100644
--- a/app.py
+++ b/app.py
@@ -344,7 +344,14 @@ _operating_intelligence_insertions = (
         '    _today_construction = _latest("portfolio_construction")\n'
         '    render_today_market_brief(briefing=briefing)\n'
         '    render_information_freshness(briefing=briefing, surface="today")\n'
-        '    render_today_opportunity_scan(briefing=briefing)\n\n',
+        '\n',
+    ),
+    (
+        '    page_header(\n'
+        '        "Current capital position",\n',
+        '    render_today_opportunity_scan(briefing=briefing)\n\n'
+        '    page_header(\n'
+        '        "Current capital position",\n',
     ),
     (
         '    latest_briefing = _latest("daily_cio_briefing")\n\n',
diff --git a/cio_report_history_ui.py b/cio_report_history_ui.py
index 9dd5e6d286b2cefcff36d095f9b2ecbd3fcabad3..ede00e5d074fd0524327489f0e37c2365d6a0620 100644
--- a/cio_report_history_ui.py
+++ b/cio_report_history_ui.py
@@ -5,6 +5,8 @@ from __future__ import annotations
 import pandas as pd
 import streamlit as st
 
+import premium_ui as ui
+
 from cio_pending_transactions import pending_transaction_report_history
 from historical_replay_ui import render_canonical_historical_replay
 
@@ -24,7 +26,11 @@ def render_cio_report_archive() -> None:
     render_canonical_historical_replay()
 
     reports = pending_transaction_report_history(limit=250)
-    st.markdown("#### CIO report archive")
+    ui.page_header(
+        "CIO report archive",
+        "Open the governed report behind each portfolio recommendation and paper-implementation state.",
+        "ARC",
+    )
     if not reports:
         st.info("No historical CIO pending-transaction reports are available yet.")
         return
@@ -50,50 +56,41 @@ def render_cio_report_archive() -> None:
         f"Report {int(selected) + 1} of {len(reports)} · "
         f"Fingerprint {str(report.get('report_fingerprint', ''))[:16] or 'unavailable'}"
     )
-    metrics = st.columns(2)
-    metrics[0].metric("Report state", _label(report.get("report_state")))
-    metrics[1].metric("Execution state", _label(report.get("execution_state")))
-    metrics = st.columns(2)
-    metrics[0].metric(
-        "Transactions",
-        int(report.get("transaction_count", 0)),
+    target_allocation = (
+        "Unchanged"
+        if no_transaction and report.get("target_cash_weight") is None
+        else _percent(report.get("target_cash_weight"))
     )
-    metrics[1].metric(
-        "Target allocation",
-        (
-            "Unchanged"
-            if no_transaction and report.get("target_cash_weight") is None
-            else _percent(report.get("target_cash_weight"))
-        ),
+    turnover = (
+        "0.00%"
+        if no_transaction and report.get("turnover") is None
+        else _percent(report.get("turnover"))
     )
-    metrics = st.columns(2)
-    metrics[0].metric(
-        "Turnover",
-        (
-            "0.00%"
-            if no_transaction and report.get("turnover") is None
-            else _percent(report.get("turnover"))
-        ),
+    expected_impro